services:
  postgres:
    image: postgres:16
    container_name: myfo-postgres
    environment:
      POSTGRES_USER: myfo
      POSTGRES_PASSWORD: myfo_dev_2024
      POSTGRES_DB: myfo
    ports:
      - "5432:5432"
    volumes:
      - myfo_pgdata:/var/lib/postgresql/data

volumes:
  myfo_pgdata:
